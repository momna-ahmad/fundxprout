-npx hardhat node : it will generate test accounts with free ether that can be considered as business accounts
-for business accounts to create campaign they actually have to pay gas fees ( adding a block with their campaign info to blockchain)
-the ethers can be deducted from these dummy accounts that are added to meta mask browser extension
-create a seed script . that can deploy the smart contract using npx hardhat run scripts/deploy.ts --network localhost 
-it will also then add dummy campaigns to it
- the address is generated by the script where the campaign factory resides and our frontend (campaign creation page) should know this address to let business add their block